import { BrowserRouter } from "react-router-dom"


export const Support = ({children}:any) => {
        return (
                <BrowserRouter>
                        {children}
                </BrowserRouter>
        )
}